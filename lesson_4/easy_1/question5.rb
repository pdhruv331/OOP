class Fruit
  def initialize(name)
    name = name
  end
end

class Pizza
  def initialize(name)
    @name = name
  end
end


# To create an instance variable the @ symbol is used. We can see that in the Pizza class